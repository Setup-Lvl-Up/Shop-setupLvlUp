{% extends "base.html.twig" %} {% block title %} {{ parent() }} Administration {% endblock %} {% block contenu %} {{ parent() }}
<div class="container">
    <div class="row">
        <div class="col-1">{% include 'partials/adminMenu.html.twig' %}</div>
        <div class="col">
            <h1>Type de produit</h1>

            {% if form.valide is defined %} {% if form.valide == true %}
            <h1 class="text-sucess">Ajout Réussi</h1>
            {% else %}
            <h1 class="text-danger">Echec de l'ajout</h1>
            <h2>Erreur : {{form.message}}</h2>
            {% endif %} {% endif %}
            <form action="index.php?page=ajout-type" method="post" enctype="application/x-www-form- urlencoded">
                <h3>Ajouter un nouveau Type</h3>
                <label for="libelle" class="sr-only">Type:</label>
                <input type="text" id="libelle" name="libelle" class="form-control" placeholder="Type" required autofocus>

                <button class="btn btn-lg mt-2 btn-primary btn-block" type="submit" name="btInserType">Ajouter</button>
            </form>
        </div>
    </div>
</div>
{% endblock %}